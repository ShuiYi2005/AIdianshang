# AGENTS

本项目遵循“平台隔离、业务集中、先规格后编码”的开发方式。

## 开发原则

- 禁止修改 `platform/` 源码。
- `services/` 是唯一业务代码目录。
- `workflows/` 只编排，不计算。
- `adapters/` 只做平台请求与业务服务之间的协议转换。
- `prompts/` 用文件管理。
- 修改前先读 `.spec/` 和 `specs/`。
- 修改后必须运行验证。
